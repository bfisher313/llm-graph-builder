# torch constraints removed - let pip resolve compatible versions
# original upstream pinned cpu-only builds for x86_64:
# -f https://download.pytorch.org/whl/torch_stable.html
# torch==2.3.1+cpu
# torchvision==0.18.1+cpu
# torchaudio==2.3.1+cpu